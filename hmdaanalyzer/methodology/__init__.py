"""Bundled methodology documents (ship inside the wheel).

The CRA-proxy methodology — including the fabrication firewall and limitations —
travels with the installed tool, accessible via
:func:`hmdaanalyzer.get_methodology_path`.
"""
